package renderer.rayTracer;

import primitives.Color;
import primitives.Ray;
import scene.Scene;

/**
 * Ray tracer types
 */
public enum RayTracerType {
   /** Simple (basic) ray tracer */
   SIMPLE,
   /** Advanced ray tracer with support for image enhancements */
    ADVANCED;
}
